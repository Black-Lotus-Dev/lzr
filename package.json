{
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm run atlas:dev\" \"pnpm run bast:dev\"",
    "atlas:dev": "cd apps/atlas && pnpm run tauri:dev",
    "bast:dev": "turbo run dev --filter bast",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "eslint": "^7.32.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^2.5.1",
    "turbo": "^1.9.4"
  },
  "packageManager": "pnpm@7.15.0",
  "name": "lzr",
  "vercel": {
    "build": {
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
}
